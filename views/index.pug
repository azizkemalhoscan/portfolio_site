extends layout.pug

block content
  article.grid-container.portfolio-intro
    div

      h1 Here, I present you my portfolio as junior software developer.

      p.lead.text-light
        | Portfolio Description
        |This portfolio is prepared to show my previous works, and to make employers comprehend who I am, what I am capable of
        | and what are my goals for the future.
  article.grid-container.portfolio-index

    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3


    each project in projects
      .cell
        a(href=`/projects/${project.id}`)
          img.thumbnail(src=`${project.image_urls[0]}` width='550' height='550')
          h5= project.project_name

